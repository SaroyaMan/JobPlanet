
export enum AccessModifier {
    Private = 1,
    Shared = 2,
    Public = 3,
}

export enum RefObjectType {
    Candidate = 1,  // Resume
    Question = 2,   // Attachment
}

export enum QuestionState
{
    General = 1,
    InMyTodoList = 2,
    InMyDoneList = 3,
    PublishedByMe = 4,
}
